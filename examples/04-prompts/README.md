# 05 — Prompts

Prompt-шаблоны с аргументами (`prompts/list`, `prompts/get`). Как они появляются в UI клиента как slash-команды и чем семантически отличаются от tools: prompt выбирает **пользователь**, tool — **модель**.

_TBD: сервер с парой prompt-шаблонов, wire-дамп, скриншот/описание того, как это видит пользователь в Claude Desktop._