# 03 — Ошибки

Два разных механизма: JSON-RPC errors (`-32xxx`, транспорт) vs `result` с `isError: true` (бизнес-логика). Когда какой применять и почему модель должна видеть бизнес-ошибки как часть разговора.

_TBD: сервер с намеренно ломающимся tool, wire-дамп обоих типов ошибок, упражнение «сломай клиента неправильной обработкой»._